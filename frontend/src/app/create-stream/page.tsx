import { KeysSettings } from '@/features/create-stream/key-settings';

const createStream = () => {
    return (
        <div>
            <KeysSettings />
        </div>
    );
};

export default createStream;
